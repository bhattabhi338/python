number = int(input("Enter a number and I will tell you it's Divisors: "))
# List of range 1 to the number that the user will input, remember here the range is from 1 to num+1 which means it will also include 'num'. 
a = list(range(1, number+1))
z = []
for e in a:
    if number % e == 0:
        z.append(e)
print(z)

